# Verification lessons — 検証そのものが嘘をついた事例

この repo には**失敗モードの台帳が 3 つ**あります。それぞれ持ち場が違います。

| 台帳 | 何を持つか |
| --- | --- |
| [`architecture-rules.md`](architecture-rules.md) | **3D シーンの構造**が静かに壊れた事例 |
| [`organ-3d-playbook.md`](organ-3d-playbook.md) | **レンダリング**の失敗モード（UV 極・両面シェル・飽和…） |
| **この文書** | **検証そのもの**が嘘をついた事例。テスト・検証スクリプト・計測 |

この文書が扱うのは、製品のバグではなく**「緑だったのに何も測っていなかった」**です。
プロダクトのバグは直せば消えます。**測る側のバグは、直るまで他の全部の信号を殺します。**

---

## 使い方 — 自己改善ループ

**間違いを 1 つ見つけたら、直す PR の中で、この台帳に 1 項目足してから次へ行く。**
別 PR に回さないでください。回した教訓は書かれません（実測: この repo で
「あとで書く」と言った教訓のうち、書かれたものは 1 件もありません）。

ループは 5 手です。

1. **ガードを書く** — テスト・検証スクリプト・アサーション、何でも
2. **壊す** — 修正を戻す / 条件を反転する / 対象を改名する
3. **赤を確認する** — 赤くならないガードは**存在していません**（L-09）
4. **戻して緑を確認する**
5. **ここに 1 項目書く** — 症状・どう見つかったか・いま何が捕まえるか

3 が要です。この台帳の A 節 8 件は**全部 2〜3 でしか見つかっていません**。
1 件も、書いた時点では間違っていると分かりませんでした。

`npm run lessons` が台帳の側を守ります——各項目が 3 つの欄を持つこと、
**名指ししたガードが実在すること**（消えたら赤くなるので、教訓を書き直すか
ガードを戻すことになります）、そして**まだ人しか捕まえられない項目が何件あるか**。
`npm test` からも同じ検査が走ります。

### 書き方

```markdown
### L-NN 1 行で「何が嘘をついたか」

- **症状**: 緑だったのに何が起きていなかったか
- **どう見つかったか**: ミューテーション / レビュー / 実機 / 偶然
- **いま何が捕まえるか**: `path/to/guard.js` — どう捕まえるか。
  機械が捕まえられないなら **人だけ**
- **一般形**: 次に同じ形が出るのはどこか（任意）
```

番号は再採番しません。ファイル全体の最大値の次を取ってください。

---

## A. 検証コードが、測っているつもりのものとは別のものを見ていた

**A 節 9 件は、ミューテーション以外では見つかっていません**（L-18 だけはレビューです）。
どれも、
書いた時点では正しく見え、レビューを通り、緑でした。

### L-01 不在の主張が、空文字列に対して通った

- **症状**: `node.text` は Playwright の `ElementHandle` には無く `undefined`。
  全行が空になり、「この文言は出ていない」という主張が**何も読まないまま**通った。
- **どう見つかったか**: ミューテーション。消したはずの文言を戻しても緑のまま。
- **いま何が捕まえるか**: **人だけ**。`textContent` を使い、`nonEmpty()` で
  「何かを読めたこと」を先に主張する形にしたが、次に同じ形を書くのを止めるものは無い。
- **一般形**: **「無いこと」の主張は、必ず「有ること」の主張とセットで書く。**
  空集合に対しては、あらゆる否定が通ります。

### L-02 正規表現が、別の規則の本体を読んだ

- **症状**: `\n\.locked-copy \{` が、グループセレクタ
  `.locked-summary,\n.locked-copy {` の**2 行目**に一致し、そちらの本体を読んだ。
- **どう見つかったか**: ミューテーション。`.locked-copy` 側の値を変えても緑。
- **いま何が捕まえるか**: `scripts/lib/css.mjs` — そのクラス名を含む規則を
  **全部**集めて最後を採る（`rulesNaming` / `fontSizePx`）。セレクタ 1 本を
  錨にしない。`tests/css-reading.test.js` がグループ選択子の実例で固定します。
- **一般形**: CSS のセレクタは行頭に無いことがあります。**グループの 2 行目以降**が
  常に待ち伏せています。

### L-03 1 つの規則の中で、最初の宣言を読んだ

- **症状**: 同じ本体に `font-size` が 2 行あるとき、勝つのは**後**。最初を読むと
  「10px が残っている」と報告し続ける。
- **どう見つかったか**: わざと control case（前が 10px・後が 12px）を置いた。
- **いま何が捕まえるか**: `scripts/lib/css.mjs` — `declaration()` が最後を返す。
  `tests/css-reading.test.js` の control case（前が 10px・後が 14px）があるので、
  `[0]` に戻すと赤くなる。
- **一般形**: カスケードは**最後が勝つ**。規則の間でも、規則の中でも。

### L-04 CSS のブロック主張が、隣のブロックに当たっていた

- **症状**: `.global-nav-brand` の border を主張していたが、一致していたのは
  同名を含む別ブロック。border を消しても緑。
- **どう見つかったか**: ミューテーション。
- **いま何が捕まえるか**: `scripts/lib/css.mjs` — セレクタを文字列として探すのを
  やめ、**規則として読みます**（`rulesOf` / `rulesNaming` / `fontSizePx`）。
  `tests/css-reading.test.js` が、この repo が実際に踏んだ 5 つの形を
  最小の stylesheet で固定し、さらに**手書きのトークナイザが 6 本目として
  生えたら赤くします**（コメントを剥がしてから探すので、それを禁じている
  説明文自体には当たりません——L-06）。
- **一般形**: L-02 と同じ根。**構造として読めるものを、文字列として探さない。**

### L-05 コメントを剥がすつもりが、本文の 93% を食べた

- **症状**: `/\/\*[\s\S]*?\*\//g` で block comment を剥がしたところ、
  `page.route('**/*')` の `/*` が開始と見なされ、**3,463 字中 3,208 字**が消えた。
  残り 255 字に対して、以降のすべての主張が通った。
- **どう見つかったか**: ミューテーション。何を壊しても緑なので、長さを印字した。
- **いま何が捕まえるか**: **人だけ**。行コメントだけを剥がす形にした。
- **一般形**: **剥がしたら長さを見る。** 前後の文字数を印字するだけで、この種の
  「入力が消えていた」は全部見えます。glob・正規表現・URL は `/*` を含みます。

### L-06 `doesNotMatch` が、それが駄目な理由を説明しているコメントに一致した

- **症状**: `assert.doesNotMatch(code, /route\.abort/)` が、
  「なぜ `route.abort` を使ってはいけないか」を書いた**コメント**に当たって赤くなった。
  主張は正しく、対象が違った。
- **どう見つかったか**: 赤くなったので。ただし逆向き（`match` がコメントに当たって
  **緑になる**）は見つかりません。
- **いま何が捕まえるか**: **人だけ**。コメントを剥がしてから測る形にした。
- **一般形**: **ソースを検索する主張は、コードに対して行う。** この repo は
  コメントが厚いので、`match` も `doesNotMatch` も、自分の説明文に当たります。

### L-07 CI の検査が、錨を下ろした 1 行の外を見ていなかった

- **症状**: `--with-deps (firefox|webkit)` に錨を下ろしたので、
  `--with-deps chromium firefox webkit` が素通りした。ファイル全体に広げたら、
  今度は**自分が書いた説明コメント**に当たった（L-06）。
- **どう見つかったか**: ミューテーション。
- **いま何が捕まえるか**: `tests/viewports.test.js` — コメントを剥がした
  `ciSteps` に対して測る。
- **一般形**: **錨は、守りたい性質ではなく、たまたま今そう書かれている形に当たります。**

### L-08 クラス名の部分一致が、entitled なコントロールを全部ロック扱いにした

- **症状**: 「class 名に lock を含む要素があるか」でロックを測った。錠前は
  **常に DOM にあり `hidden` になるだけ**なので、全件がロック扱い。
- **どう見つかったか**: entitled 側も `--locked` 側も同じ答えを返したので。
- **いま何が捕まえるか**: `scripts/check-gated-surfaces.mjs` — 製品自身の信号
  （`is-locked` かつ `.feature-lock:not([hidden])`）を読む。
  `tests/preview-grants.test.js` が、その 2 つを読んでいることを固定する。
- **一般形**: **製品が自分で持っている状態を読む。** 「それらしい class 名がある」は
  状態ではありません。

---

### L-17 L-01 が、L-01 を止めるために書いた file の中で再発した

- **症状**: この台帳のガードに「文書のテンプレートは lesson として数えない」という
  テストを書きました。`assert.ok(parseLessons(ledger).every((l) => l.id !== 'L-NN'))`。
  **パーサが何をしても通ります**——見出しの正規表現は `L-\d+` なので、
  `L-NN` は最初から一致しません。**空集合に対する不在の主張**（L-01）です。
- **どう見つかったか**: ミューテーション。fence の処理を `if (false)` に潰しても
  緑のままだったので。**7 つ壊したうち、これだけが緑でした。**
- **いま何が捕まえるか**: `tests/lessons.test.js` — 囮を**実在する番号**
  （`L-07`）で fence の中に置き、パース結果が `['L-07']` **と等しい**ことを主張する。
  `deepEqual` なので、数え過ぎも数え落としも赤くなります。
  fence を潰すと赤、文書側のテンプレートを fence から出しても赤。
- **一般形**: **「X が無い」は、「Y が有る」と同じ assert で書く。**
  `every(... !== x)` ではなく `deepEqual(all, [expected])`。
  そして——**教訓を書いた当人が、その教訓を、その場で繰り返します。**
  だからループの 3 手目は、書いた本人にも要ります。

### L-18 「欄が有るか」を見て、「何か書いてあるか」を見なかった

- **症状**: `npm run lessons` が必須 3 欄を `fields.has(name)` で検査していました。
  `- **症状**:` と書いて中身を空にすると、パーサは空文字列を格納し、
  `has()` は `true` を返します。**3 つの事実を 1 つも述べていない項目に対して、
  台帳は「well-formed」と報告します。** 内容の検査を受けていたのは
  ガード欄だけでした。
- **どう見つかったか**: **レビュー（Codex, P2）**。ミューテーションでは出ていません
  ——私が壊したのは `has()` の**結果**（欄を丸ごと消す）であって、
  **欄が有るまま中身が空**という状態を作っていなかったからです。
- **いま何が捕まえるか**: `tests/lessons.test.js` — 空欄 2 つを持つ項目に対して
  `problems` が**その 2 件と正確に一致する**ことを `deepEqual` で主張し、
  併せて**折り返した次の行から本文が始まる項目は通る**ことも主張します
  （空の検査をパース中に行うとこちらを誤って拒否します）。
  検査を `false` に潰しても `true` に潰しても赤くなります。
- **一般形**: **「有るか」と「何か言っているか」は別の主張です。** L-01 の同型
  ——あちらは空文字列に対して不在が通り、こちらは空文字列に対して存在が通りました。
  そして**ミューテーションにも死角があります**: 壊し方が 1 種類だと、
  その軸でしか測れません。**空・ゼロ件・空白だけ**は、消すのとは別に試してください。

### L-19 ミューテーションが、囮の**並び順**のせいで素通りした

- **症状**: 「`.copy` は `.copy-inner` に一致しない」を、
  `.copy-inner { 9px } / .copy:hover { 15px }` という stylesheet で主張しました。
  部分一致に壊すと**両方の規則が集まりますが、最後が勝つので答えは 15px のまま**。
  **アサーションの結果が、それが名指ししている性質に依存していませんでした。**
- **どう見つかったか**: ミューテーション。6 件中 1 件だけが緑で、
  ヘルパーの `namesClass` を `includes()` に潰しても誰も気づきませんでした。
- **いま何が捕まえるか**: `tests/css-reading.test.js` — 囮を**後ろに**置き
  （部分一致なら 9px が答えになる）、併せて
  `rulesNaming(...)` が `['.copy:hover']` **と一致する**ことを `deepEqual` で主張します。
- **一般形**: **囮は、混入したら答えが変わる位置に置く。** L-17 と同じ形
  ——あちらは空集合、こちらは「最後が勝つ」規則に隠れました。
  ミューテーションを 1 件だけ書いて緑なら、**まず囮の置き方を疑ってください。**

### L-20 「移しただけ」の統合が、4 つの挙動を静かに変えた

- **症状**: 5 本の手書き CSS トークナイザを 1 本に寄せたとき、**等価性の根拠として
  type-floor の baseline（33 シート・209 宣言）が完全一致することを示しました。**
  それでも 4 つの挙動が変わっていました:
  1. `font-size: 10px !important` が **px として数えられなくなった**
     （＝床をすり抜ける）
  2. `.copy span` が `.copy` を名指していると見なされ、`.copy { 10px }` の後に
     `.copy span { 16px }` があると**床検査が 16px で通る**
  3. 「`@` で始まる selector は飛ばす」という**私が足した安全策**が、
     `@import` は `;` で終わって次の規則の selector 塊に**畳み込まれる**ため、
     `browser-first-release-polish.css` の**最初の実規則を丸ごと捨てていた**
  4. `z-index` が `--overlay-z-index` の中に一致し、後者が前者の代わりに答える
- **どう見つかったか**: **レビュー（Codex, P2 × 4）**。私のミューテーション 8 件は
  1 件も捕まえていません。
- **いま何が捕まえるか**: `tests/css-reading.test.js` — 4 つそれぞれに最小の
  stylesheet を置き、4 方向に壊して赤を確認済み。
- **一般形**: **現在のデータでの等価性は、現在のデータが通らない経路の変更を
  証明しません。** 209 が一致したのは、4 つのどれも今の `src/styles/` に
  たまたま存在しなかったからです。統合するときは、
  **移した先の入力空間**（`!important`・子孫選択子・`@import`・`--` 接頭辞）を
  数え上げてください——移す前のコードが何を受け取っていたか、ではなく。
  そして **「念のため」で足した早期 return は、データを消します。**
  3 は元のコードには無く、私が足した 1 行です。

### L-22 マージがコード表を**コメントの中に**移し、検査はそれを読み続けた

- **症状**: `scripts/check-anatomy-interaction.mjs` の `SCENE_POINTS` に入って
  いるはずの heart と brain の「名前つき tour」が、マージで**表の直上の doc
  コメントの中**に入りました。drive はそれを読めないので
  `no authored click tour for heart-anatomy` と報告して**計測した 4 点を代わりに
  clicked**——つまり公開記録が名指ししている 4 構造は誰も確かめていません。
  それでもテストは緑でした。`tests/beta-release.test.js` はスクリプトを
  **ファイル全体のテキストとして**読んで `'heart-anatomy': [` を探しており、
  **コメントの中の 4 つの名前を見つけて**「scope と tour は一致している」と
  答えていたからです。
- **どう見つかったか**: **実行して出力を読んだ**。`npm run verify:anatomy --
  --scene heart-anatomy` の note 行。テストからは 1 文字も出ていません。
- **いま何が捕まえるか**: `tests/beta-release.test.js` — 検索範囲を
  `const SCENE_POINTS = {` から閉じ括弧までに限定したうえで、**名前つき tour を
  持つ公開シーンの一覧を `deepEqual` で固定**しています。範囲を絞るだけでは
  足りません（tour が消えると「無い」に落ちて、`if (!names) continue` で
  静かに読み飛ばされる）。rows をコメントに戻す mutation で赤くなることを
  確認済みです——**範囲を絞っただけの版は緑のままでした**。
- **一般形**: **ソースをテキストとして読む検査は、コメントアウトされた自分の
  期待値を読みます。** 範囲をまず構文単位に絞る。そして
  **「無ければ読み飛ばす」は、消えたことを検出できない**——読み飛ばす対象は
  一覧で固定する。L-04（正規表現が別の規則に当たる）と L-01（不在が空集合に
  通る）が同時に出た形です。

### L-23 計測器が、測った名前をコメントにだけ書いて捨てていた

- **症状**: `scripts/measure-anatomy-points.mjs` は各点が**何に当たったか**を
  知っています（同じ名前の候補を弾くのにそれを使っています）。なのに
  貼り付け用の行は `[fx, fy]` の裸の組で出力し、**名前は上のコメントにだけ**
  書いていました。`check-anatomy-interaction.mjs` は 3 番目の要素がある点しか
  構造名に対して検査しないので、**この loop が出したどの表も、貼った瞬間から
  検証されていません**。`SCENE_POINTS` の座標のみ 35 件はここから出ています
  ——F-123 そのものの供給源です。
- **どう見つかったか**: **レビュー（Codex, P1）**。PR #112 で、この loop を
  含む diff に対して。私はこの PR で F-123 を「次にやる作業」として読んで
  いながら、**その原因が同じ diff の中にあることに気付いていませんでした**。
- **いま何が捕まえるか**: `tests/beta-release.test.js` — 出力行が
  `[${point.fx}, ${point.fy}, ${quoted(point.name)}]` の形であることを
  ソーステキストに対して主張します。裸の組＋コメントに戻す mutation で
  赤くなることを確認済みです。
- **一般形**: **測った値をコメントに書くのは、捨てるのと同じです。**
  コメントは何にも読まれません（脳の 4 点は 1 週間ずれたまま、コメントだけが
  正しいふりをしていました）。**計測器の出力は、検査器の入力の形で出す。**
  2 つの道具の間にある「人が読み替える一手」が、ずれの入り口です。

### L-36 閉じた `<details>` の中身は、計算スタイル上は「見えている」

- **症状**: 「この要素は読者に見えているか」を、**祖先を辿って
  `display:none` / `visibility:hidden` / `opacity:0` が無いか**で判定しました。
  閉じた `<details>` の中の `<a>` は、そのどれにも当たりません——
  `getComputedStyle` は `display:block`、`getBoundingClientRect` は 313×53 を返し、
  `offsetParent` も非 null です。**チェックは「見えている」と答え、
  スクリーンショットには何も写っていませんでした。**
  同じ調査で `innerText` は `''`（＝描画されていない）、`textContent` には文言あり。
  空の `innerText` を見て「ラベルの無いリンクだ」と**報告しかけました**（L-01）。
- **どう見つかったか**: **スクリーンショットを見た**から。3 つの自動読み取りが
  すべて別の嘘をつき、画像だけが「循環器が畳まれている」と教えました。
- **いま何が捕まえるか**: **人だけ**。到達可能性は
  `document.elementFromPoint(cx, cy)` が**その要素か子孫を返すか**で見ます
  （閉じた `<details>` では祖先のスクロール容器が返る）。Playwright の
  `click()` がタイムアウトするのも同じ信号です。
- **一般形**: **「描画されているか」を計算スタイルの walk で判定しない。**
  `<details>`・`content-visibility`・`hidden until-found`・clip-path は、
  祖先のどのプロパティにも現れません。**製品自身の当たり判定を使い、
  仕上げに画像を見る。** そして——
  **自動読み取りが 3 つとも違うことを言ったら、目で見るのが一番速い。**

### L-37 スクロールしない箱に入れた「最上部」は、最上部ではない

- **症状**: ドロワーの最上部に近道の一覧を足しました。`.global-nav-panel` は
  `overflow: hidden` の縦 flex で、**スクロールする子は `.global-nav-list` だけ**。
  その兄弟として置いたので、一覧が伸びると**切り落とされます**。
  390px で一覧を自分の上限（16 件）まで伸ばすと、セクションは 955px、
  パネルは 720px——**7 行が、どうやっても届かないところ**にありました。
  4 件の実機確認では出ません。上限まで伸ばして初めて出ます。
- **どう見つかったか**: **レビュー（Codex, P2）**。私は「公開中の 4 件」でしか
  測っておらず、**自分で書いた上限の側を測っていませんでした**。
- **いま何が捕まえるか**: `tests/scene-switcher-models.test.js` — 近道が
  **スクロールする要素の中の先頭**であることを主張します（パネルの直下に
  無いことも併せて）。外に戻すと赤、中でも末尾に置くと赤。
- **一般形**: **「上に置いた」は「届く」ではありません。** そして——
  **自分で決めた上限は、自分で測る。** 境界値を書いたなら、その境界で
  1 回動かしてください。実データがまだ境界から遠いときほど、そこが盲点になります。
  可視性は「いまの位置で見えるか」ではなく、**「スクロールして届くか」**で測ります
  （この修正の前後で `false` → `true`。測り方を直すまで、両方とも `false` に見えました）。

### L-46 「まだ人しか捕まえられない」を note に格下げしたまま、赤にする条件を書かなかった

- **症状**: 脳解剖シーンで、タップした構造の名前がパネルには出るのにモデル上には
  出ないことがありました。選択のラベルのアンカーは landmark と同じ、カメラに依らない
  固定の 1 頂点（`outwardSurfacePoint`）で、溝（sulcus）ではその頂点が隣の回に
  隠れることが F-40 として**既に記録済み**でした。それでも `verify:anatomy` は
  この状態を検出できていました——`labelled.includes(pinnedForLabel)` が偽のとき、
  `problems.push` ではなく `notes.push` していたからです。**チェックは正しく
  「ラベルが無い」と測っていながら、それを失敗として報告しない一行があったので、
  緑のまま出荷されました。** 何も嘘をついていた計器はなく、「測ったものを赤にする」
  配線が欠けていただけです。
- **どう見つかったか**: 実機（phone 390×844・desktop）でのスクリーンショット比較。
  選択前は landmark の「中側頭回」がモデル上に浮き、選択後もパネルは「島輪状溝」を
  名乗るのにモデル上の文字は変わりませんでした——画像を並べて初めて気づく種類の
  食い違いで、`verify:anatomy` のログはその回では何も報告していませんでした。
- **いま何が捕まえるか**: `scripts/check-anatomy-interaction.mjs` の 3c
  （`the pinned structure "..." has no label on the model from this angle` の
  行）を `notes.push` から `problems.push` に変え、選択直後のラベル欠落を
  hard failure にしました。加えて `tests/label-layer.test.js`
  （選択はラベル上限の対象外になること、landmark が muted で選択と重複しないこと）と
  `tests/brain-anatomy-selection-label.test.js`（`_visibleAnchorFor` が
  遮蔽された候補より見える候補を選ぶこと、タップした点をそのまま使うこと）が
  `node --test` だけで壊れた組み合わせを再現し、実装側の修正
  （`BrainAnatomyScene._visibleAnchorFor` / `LabelLayer.js` の cap 除外と
  landmark マージ）を戻すとどちらも赤くなることを確認済みです。
- **一般形**: **「まだ捕まえられていない」ことを note として残すのは、
  そのこと自体は正しい判断です。** けれど note に落とした条件がいつまでも
  `problems` に昇格されないままだと、「知られている既知の欠陥」が
  「チェック済みで問題なし」と区別できなくなります。F-40 のような
  ラベル分の note は、**直すたびに「これで assert に格上げできるか」を
  問い直してください。**

## B. ガードが、存在しないか、届いていなかった

### L-09 編集スクリプトが途中で assert し、何も書かずに終了した

- **症状**: テストを追加する編集スクリプトが、途中の整合性 assert で落ちて
  **1 バイトも書かずに** exit した。ファイルは完成して見え、
  `?entitled=` の真理値表テストは**存在しないまま**、3 つのミューテーションが素通りした。
- **どう見つかったか**: ミューテーション。「通るはずのない変更」が 3 つ通ったので、
  テストを grep したら無かった。
- **いま何が捕まえるか**: **人だけ**。
- **一般形**: **書いたつもりのガードは、壊して赤を見るまで存在していません。**
  これがループの 3 手目が要である理由です。編集スクリプトは
  「書いた」ではなく「書いたものを読み直して」確認してください。

### L-38 台帳の「人だけ」の数が、半分直すたびに黙って下がっていた

- **症状**: `npm run lessons` が数える「まだ人しか捕まえられない項目」は、
  **ガードを 1 つでも名指した瞬間にその項目を数から外します**。
  ところが項目は「片方は `npm run lessons` が捕まえる、もう片方は **人だけ**」と
  両方を書けます（L-16 がまさにそれです）。結果、**何も作っていないのに数が 1 減る**。
  実際 4 件（L-16 / L-21 / L-27 / L-30）が既にそうやって抜けており、
  正しい数は 12 ではなく **15** でした。
  さらに同じ実行が**画面上で矛盾**していました——行のマークは「`人だけ` と
  書いてあるか」で付き、集計は「ガードを名指していないか」で数えるので、
  `human` と印字された 15 行の下に「12 件」と出ていました。
  **CLAUDE.md が「項目を消して下げるのは逆走」と呼んでいるものの、
  消さずに下げる版です。**
- **どう見つかったか**: レビュー。L-16 に「lessons.mjs が半分は捕まえる」と
  書き足したら数が減り、**足したのに減った**のが手がかりでした。
  F-125 が「再読でも 12 件のまま」と書いていたのも、同じ欠陥を見ています。
- **いま何が捕まえるか**: `scripts/lib/lessons.mjs` — 被覆を
  `guarded` / `partly` / `human` の 3 値にし（`coverageOf`）、
  **`partly` は `human` と一緒に数えます**。行のマークと集計は同じ判定を読みます。
  `tests/lessons.test.js` が 3 通のミューテーション（3 値をやめる／
  `partly` を数から外す／ラベルを消す）で赤になることを確認済み。
- **一般形**: **「まだ埋まっていない数」を数える計器は、半分埋めたときに
  何が起きるかで設計する。** 2 値にすると、部分的な前進が全部の前進として
  記録されます。そして**同じ実行の中の 2 つの数字は、同じ 1 つの判定から出す。**

### L-43 直した規則は、勝っていた規則ではなかった

- **症状**: WebKit だけコントロール列がはみ出す件（F-137）で、`ui.css` の
  `#ui[data-anatomy] .button-row` から `flex-wrap: nowrap` を外し、
  「これで折り返すはず」と考えて出しました。**computed value は `nowrap` のまま**です。
  `anatomy-shell-presentation.css` に
  `#ui[data-anatomy-shell='calm'] .console .button-row { flex-wrap: nowrap }` が
  別にあり、詳細度で勝っていました。CI を 1 往復（約 3 分）使って、
  **同じ赤・同じ数値**が返ってきて初めて気づいています。
- **どう見つかったか**: 直したのに数値が 1px も動かなかったこと。
  そこで**ブラウザに「この要素に `flex-wrap` を当てている規則を全部挙げろ」と
  聞きました**——`document.styleSheets` を歩いて `element.matches(selectorText)`
  で絞る 15 行です。3 件出てきて、勝っていたのは私が触っていないほうでした。
- **いま何が捕まえるか**: **人だけ**。`scripts/lib/css.mjs` は規則を読めますが、
  「どの規則が勝つか」は詳細度・出現順・media の入れ子の解決なので、
  静的には答えられません（ブラウザに聞くしかない）。
- **一般形**: **CSS を直したら、直した規則ではなく computed value を確かめる。**
  同じ要素に当たる規則は複数あり、grep で見えるのは**書いてある場所**であって
  **効いている場所**ではありません。1 つのファイルで見つけて満足した時点が危険です。
  効いている規則を知りたければ、**ブラウザに聞く**のがいちばん速い
  （`styleSheets` × `matches()`）。

### L-44 5 つ宣言した戻り道のうち、ガードは 1 つしか投げていなかった

- **症状**: 「UI を隠す」から戻る道として `App.js` は 5 つの入力
  （`pointermove` / `pointerdown` / `touchstart` / `wheel` / `keydown`）を
  宣言しています。`hideUiRoundTrip()` はそのうち `pointermove` と `touchstart` だけを
  動かしており、**残り 3 つはリスナー一覧から消しても `npm test` も
  3 エンジンの往復検査も緑のまま**でした。そのうえで台帳（F-136）に
  「機械で確かめられる分は終わっている」と書いています。
  ガードは 1 つ壊して赤を見た（L-32 のとおり）ので、**存在はしていました**
  ——足りなかったのは**網羅**のほうで、赤を 1 回見たことが
  「宣言したぶん全部を見ている」の証拠になっていません。
- **どう見つかったか**: レビュー（Codex）が
  「`keydown` を消しても全部緑のままだ」と指摘しました。自分の計測ではありません。
- **いま何が捕まえるか**: `scripts/check-viewports.mjs` の `wokenByEach()` が、
  投げられる 4 つを**1 つずつ**投げて、そのハンドラの中でクラスを読み、
  答えなかったものを名指しで報告します（`pointermove` は実マウスで別に運転）。
  `App.js` のリスナー一覧から `keydown` を消すと
  「keydown (left it faded)」で赤、`wheel` も消すと 2 件とも名指しで赤、
  戻して緑を確認しました。
- **一般形**: **1 つ壊して赤を見たら、それは「そのガードが動く」の証拠であって、
  「宣言した範囲を覆っている」の証拠ではありません。** 対象が列挙できるもの
  （イベント種別・viewport・部位・状態）なら、ガードの側も**列挙から回す**——
  1 件を代表として選んだ時点で、残りは測っていないのと同じです。
  そして台帳に「機械で確かめられる分は終わった」と書く前に、
  **その「分」が何件で、ガードが何件投げているかを数えてください。**

### L-10 報告の順序が、原因を隠した

- **症状**: 「どの paid surface も開かなかった」を、**理由を説明する problems より
  先に**出力して return していた。出力は「build が壊れている」と読め、
  実際の理由（`features` が fail-closed）はその下に印字されるはずだった行にあった。
  build 側を 4 往復疑った。
- **どう見つかったか**: 4 回目にスクリプトを読んだ。
- **いま何が捕まえるか**: `scripts/check-gated-surfaces.mjs` — problems を常に先に出す
  （その旨のコメント付き）。
- **一般形**: **要約より先に理由を出す。** 早期 return の前に、集めた問題を吐く。

### L-31 「UI を隠す」から戻るボタンを、誰も一度も押していなかった

- **症状**: `#ui.is-hidden > *:not(.ui-toggle):not(.label-layer)` の
  `:not(.ui-toggle)` は**何にも一致していませんでした**。ボタンは `#ui` の子ではなく
  `.top-bar > .rail > .rail-buttons` の 3 階層下にあり、子結合子が見ていたのは
  `.top-bar` だけです。結果、「UIを隠す」を押すと**戻すためのボタンごと**消え、
  画面上のどこにも書いていない `H` キーだけが出口でした。除外が書いてあるので
  読むと正しく見えます——**一致しない `:not()` は、意図を宣言したまま黙る**。
- **どう見つかったか**: 利用者の画面録画。`verify:ui` は緑で、
  このボタンを**一度も押していませんでした**。`shots:anatomy` は押していましたが、
  **Playwright は `opacity: 0` の要素を「見えている」と答えて click する**ので、
  人には押せないボタンを押して 1 リリースぶん通り続けました。
- **いま何が捕まえるか**: `scripts/check-viewports.mjs` — `hideUiRoundTrip()`。
  scene surface で往復させ、**隠れている間に**そのボタンを
  「自分の computed `visibility`」「祖先まで掛け合わせた `opacity`」
  「`elementFromPoint` が誰に click を渡すか」の 3 つで測り、戻れることまで見ます。
  `isVisible()` は使いません（それが今回の嘘です）。
  修正を戻すと `opacity 0.00` を名指しして赤になります。
- **一般形**: **終了状態を作るコントロールは、その終了状態の中で測る。**
  そして `opacity` は祖先で潰されたら子では戻せません——子に戻す権利がある
  プロパティは `visibility` です（`#ui.is-story` は既にそう書いていました）。
  **実ブラウザ検証の「見えているか」は `isVisible()` では足りない。**

### L-32 新しいガードの「赤い側」を、一度も通っていなかった

- **症状**: L-31 のガードを足したとき、**緑になることだけ確かめて出しました**。
  赤い側には 2 つ嘘がありました。(1) 後始末の `keyboard.press('h')` が
  **効きません**——押した直後のボタンに focus が残っており、`#ui` 内の
  interactive な要素からの keydown は `installUiShortcutGuard` が window に
  届く前に止めます。つまり**このガードが捕まえるはずの失敗のときだけ**、
  UI が隠れたまま tab 巡回・タッチ目標・遮蔽の測定に入り、1 件の指摘が
  画面いっぱいの偽の指摘になるところでした。(2) WebGL2 の無いエンジン
  （CI の Firefox）では renderer fallback が出てそもそもボタンが無く、
  「コントロールが無い」と**機械の問題を製品の欠陥として**赤くしていました。
  この file は同じ事象を他の検査では全部 note に落としています。
- **どう見つかったか**: レビュー。緑しか見ていなかったので、自分では
  見つかりませんでした。
- **いま何が捕まえるか**: `scripts/check-viewports.mjs` — `restore()` が
  focus を外してから `h` を押し、それでも駄目ならクラスを直接外します。
  fallback は `rendererDown()` で note に落とします。修正を戻して赤にすると、
  問題は**1 件だけ**報告され、緑のときと同じ note しか出ないことを確認済み。
  fallback 側は `--disable-3d-apis` で実際に走らせて note になることを確認。
- **一般形**: **ガードは、緑の経路と赤の経路の両方を通してから出す。**
  ループの 2 手目（壊す）は「赤くなるか」だけでなく
  **「赤くなった後どうなるか」**を見るためにもあります。
  後始末・fallback・タイムアウトは、**赤い側にしか無いコード**です。

### L-33 バグ報告は満たしたのに、その機能が在る理由を壊していた

- **症状**: L-31（「UI を隠す」の戻りボタンが一緒に消える）を直し、戻れることを
  測って緑にしました。**その機能は「キャプチャ用に UI を隠す」です。**
  戻りボタンを出したままにしたので、以後どのスクリーンショット・画面録画にも
  隅にボタンが写ります。報告された不具合は消え、**機能が存在する理由のほうが
  壊れました**。しかも `is-capture`（スクリプト用の抜け道）は
  `capture-anatomy-views.mjs` の側にしか無く、人間には等価物がありませんでした。
- **どう見つかったか**: 監査。私が書いたテストも `verify:ui` も、
  **「戻れるか」しか聞いていません**でした。機能の目的を聞く行が 1 行も無い。
- **いま何が捕まえるか**: `tests/hide-interface.test.js` — 戻り道が hide を
  生き延びること**と**、`is-quiet` で枠から下がること、下がっても hit test 上は
  押せること、`is-capture` がスクリプト専用であることを、`scripts/lib/css.mjs` で
  規則として読んで固定します（9 通のミューテーションで赤を確認）。
  実ブラウザ側は `scripts/check-viewports.mjs` の `hideUiRoundTrip()` が
  4 つの問い（出る／独りでに下がる／ポインタ 1 回で戻る／押すと戻る）で測ります。
- **一般形**: **バグ報告は仕様ではありません。** 直すときは
  「報告された症状が消えたか」と「**この機能は何のために在るのか**」を
  両方測る。片方だけのガードは、もう片方を壊す許可証です。

---

### L-24 探針が、どの点がどの答えを出したのかを言わなかった

- **症状**: `--points` に 21 点を渡して掃いたところ、返ってきたのは
  **18 個の名前**でした。`structures named by click` は**当たった点しか**
  並べないので、外れた 3 点が黙って落ち、名前と座標の対応が付きません。
  「8 番目と 9 番目が LAD」と分かっても、それがどの fx なのかは数えるしかなく、
  数えた答えは解剖学的に辻褄が合いませんでした（溝の左右が入れ替わる）。
- **どう見つかったか**: 対応を数えようとして、結果が矛盾したから。
  **1 回 13 分の実行を 1 本無駄にしています**（この session で計測器の
  報告不足に払った 3 本目）。
- **いま何が捕まえるか**: **人だけ**——ただし原因は消しました。
  `check-anatomy-interaction.mjs` が `point by point:` として
  **外れた点も含む全点**を `fx, fy -> 名前` で出します。`tour` 配列は
  最初から全点を持っていて、印刷されていなかっただけです。
- **一般形**: **「見つかったものの一覧」は、探索の結果ではありません。**
  探針の出力は**入力と同じ基数**で出す——N 点を渡したら N 行返す。
  当たりだけを返す API は、呼び出し側に index の再構成を強いて、
  そこで嘘が入ります。L-23 と同根です（計測器の出力を、検査器が読める形で出す）。

### L-25 推論で選んだ許容値が、無視するはずのものを飲み込んだ

- **症状**: 再構図の「読者がカメラを触ったか」判定を、厳密比較から
  `max(1e-3, distance * 0.005)` に緩めました。根拠はコメントに書いたとおり
  **「意図的な zoom の最小刻みよりはるかに小さい」**——これは測っていません。
  実際には、ホイール 1 刻みは damping 越しに徐々に効くので **zoom の開始直後は
  まだ 0.5% 以内**にいます。そこで band が変わると watcher が
  「触られていない」と判定し、**読者の zoom ごとカメラを吸着**しました。
  公開中の `brain-anatomy` で、390x844 の zoom が被写体を **20px** 動かします
  （anchor が効いていれば 0px）。
- **どう見つかったか**: `verify:anatomy` を公開シーンで回したから。
  **テスト 2583 件は 1 件も赤くなっていません。** 自分が原因かどうかは、
  `origin/main` を git worktree に出して同じ drive を回して確定させました
  （main は `exit=0`）。犯人の特定は 2 変更を 1 つずつ戻す二分探索です。
- **いま何が捕まえるか**: `scripts/check-anatomy-interaction.mjs` の zoom 4 件
  ——**ちょうど 0px** を期待します。修正は撤回し、F-133 を再オープンしました。
- **一般形**: **閾値は測ってから書く。** 「A は B よりずっと小さい」と
  コメントに書けてしまうことと、実際にそうであることは別です。
  そして今回は**そもそも閾値が存在しませんでした**——damping の残差と
  zoom の立ち上がりは同じ量で、分離できません。
  **分けられない 2 つを閾値で分けようとしていないか**を先に疑ってください。
  L-15（確かめる前に主張した）の、数値版です。

### L-26 計測器と検査器が、同じシーンを別の瞬間に測っていた

- **症状**: `points:anatomy` が掃引で出した 4 点を `SCENE_POINTS` に貼り、
  `verify:anatomy` を回すと **掃引が直前に確認した点で「何にも当たらない」**。
  同じシーン、同じ viewport（1280x800）、同じ日。2 つの計器が矛盾しました。
  原因は**測る瞬間**です——掃引はシーンが開いた直後で測り、drive は step 0 の
  framing 検査（表示のリセットを含む）を経たあとで tour を実行します。
  `lung-anatomy` は開いた直後 0.22..0.50、落ち着いて 0.20..0.52 と**構図が
  違う**ので（F-133）、掃引の座標は drive の frame では別の場所を指しました。
- **どう見つかったか**: 再計測した tour が、再計測したのに落ちたから。
  `brain` と `liver` では 2 つの構図が一致していたので**露見しませんでした**
  ——つまりこの不一致は、ずれるシーンが来るまで何年でも隠れられます。
  なお途中で main と比較しようとして 10 分無駄にしています：**比べようとした
  「開始構図 vs reset 構図」の検査自体がこちらの追加**で、main にはその行が
  存在しませんでした（比較にならない比較）。
- **いま何が捕まえるか**: **人だけ。** `SCENE_POINTS` の lung・knee・shoulder の
  項に、この scene は drive で測ること、掃引を使ってはいけないことを書いています。
  機械的には、2 つの計器が同じ瞬間を見ているかを主張する検査が
  要りますが、まだありません。
  **2026-09-16 追記: 膝と肩でも同じことが起きました。** 掃引は膝の
  (0.365, 0.37) を "Patella" と呼び、drive は同じ点で "Quadriceps tendon" と
  答えます——**丸ごと 1 行ぶんずれて**、下まで同じだけずれています。
  肩は掃引の 3 点のうち 2 点が drive では何にも当たりません。
  肺 1 件のときは「ずれるシーンが来るまで隠れられる」と書きましたが、
  **3 件目です。もう例外ではありません。**
- **一般形**: **「同じものを測っている 2 つの計器」は、同じ*時点*を見ている
  とは限りません。** 片方が定常状態、片方が初期状態を測っていると、
  両者が一致するケースでは何年でも気づけず、ずれるケースで初めて出ます。
  値を突き合わせる前に、**いつ測ったのかを突き合わせてください。**
  L-25 が「閾値は測ってから」なら、これは「**測定は時点まで含めて測定**」です。

## C. 実ブラウザ検証の運転

### L-11 「着地しないリロード」を作る方法は `204` しかなかった

- **症状**: `route.abort()` はナビゲーションを**エラーページに差し替える**ので、
  文書ごと消えます。pending のまま放置すると teardown が止まります。
  そして `page.evaluate` には**タイムアウトが無く**、ナビゲーション保留中は
  スクリプトが走りません（Chromium は defer します。CDP の `Runtime.evaluate` も同じ）。
- **どう見つかったか**: 3 通り試して全部ハングした。
- **いま何が捕まえるか**: `scripts/check-departure.mjs` — `204 No Content` で応答する。
  ナビゲーションは放棄され、**文書は生きたまま script が動く**。
- **一般形**: 「ナビゲーションが始まったが終わらない」を作りたいときは `204`。

### L-12 計測値をナビゲーション越しに返すと、速いマシンで壊れた

- **症状**: ローカルは緑、CI で "Execution context was destroyed"。
  `page.evaluate` の戻り値がナビゲーションを跨いでいた。
- **どう見つかったか**: CI。
- **いま何が捕まえるか**: `scripts/check-departure.mjs` — `sessionStorage` に置き、
  `documentElement.dataset.route` が目的の値になるのを待ってから読む。
- **一般形**: **ナビゲーションを跨ぐ値は、文書ではなく storage に置く。**

### L-13 実ブラウザ検証を 2 本同時に走らせて OOM kill された

- **症状**: 1 コマンドに 2 本繋いだら exit 137。software GL は 1 本で 4 コアを使い切ります。
- **どう見つかったか**: exit 137。落ちたのは検証そのものではなく、走らせ方でした。
- **いま何が捕まえるか**: **人だけ**（`CLAUDE.md`「同じ車輪を二度作らない」に記載済み）。
- **一般形**: 1 本ずつ。遅いときは `ps -eo pid,ppid,time,comm --sort=-time | head` で
  PPID 1 の孤児 `chrome` を疑う。

### L-14 固定の待ち時間で測った値は嘘をついた

- **症状**: 拡大の支点を 600ms 後に測って「124px ずれている」と報告したが、
  実際は同意カードを閉じた直後の framing tween の最中だった。止まるまで待つと 0px。
- **どう見つかったか**: 「124px ずれている」が再現しなかったので、待ち方を変えた。
- **いま何が捕まえるか**: **人だけ**（`CLAUDE.md` に記載済み）。
- **一般形**: **時間ではなく状態を待つ。**

---

## D. 主張と番号

### L-34 CDP の 2 往復は「同じ瞬間」ではなかった

- **症状**: 製品側に 2.2 秒のタイマーがあり（無操作なら戻りボタンが枠から下がる）、
  検証は「マウスを動かす → 見え方を測る」を**2 回の CDP 呼び出し**で書いていました。
  この runner は software GL で脳アトラスを描くので、**メインスレッドが 2.2 秒以上
  止まります**——実測。結果、呼び出しの合間にタイマーが発火し、
  検証は**runner の停滞を stylesheet の欠陥として**赤く報告しました。
  同じ形でもう一度踏みました: click 直後に測ったので、
  **Playwright が置いていったマウスの hover** が「まだ光っている」の理由でした。
- **どう見つかったか**: 自分のガードが緑のはずの木で赤くなった。1 回目は
  原因を推測せず `getComputedStyle` の鎖を印字して確かめました（L-15）。
- **いま何が捕まえるか**: `scripts/check-viewports.mjs` — 状態の読みは
  **1 つの `evaluate` の中**で（クラスを自分で立て、両方の状態を読み、戻す）、
  「ポインタで戻るか」は**そのイベントハンドラの中**で記録します。
  測る前に pointer と focus を退かせます。タイマーは割り込めません。
- **一般形**: **相手にタイマーがあるなら、質問と答えを同じ task に入れる。**
  L-14 は「時間ではなく状態を待て」でした。これはその 1 段下——
  **状態を 2 回に分けて読むと、2 回のあいだに状態が変わります。**
  そして**測る前に、自分の道具が残していったもの（マウス・focus）を片付ける。**

### L-35 `waitForFunction` は rAF で polling する——その rAF を飢えさせているのが自分だった

- **症状**: 上の L-34 を直した直後、`desktop-1280` **だけ**で
  「押しても UI が戻らない」が断続的に出ました。**戻っていました。**
  `page.waitForFunction` の既定の polling は `requestAnimationFrame` で、
  この runner は software GL で脳アトラスを描いています。つまり
  **測る側の polling 周期を、測られている側が決めていました**——
  1280×800 では frame 間隔が伸び、5 秒の上限が実質 3〜4 回の試行になります。
  最大の canvas を持つ viewport でだけ落ちたのはそのためです。
- **どう見つかったか**: 3 viewport の run で落ち、単独 run では緑。
  「flake」で済ませずに、落ちるのが常に最大 viewport であることを見ました。
- **いま何が捕まえるか**: `scripts/check-viewports.mjs` — クラス待ちは
  `{ polling: 250 }`（interval）と、この file の他の待ちと同じ余裕のある上限で
  行います。修正後に同じ 3 viewport を 3 回連続で緑にして確認。
- **一般形**: **3D シーンを測るとき、`waitForFunction` の既定 polling は使わない。**
  rAF は描画に律速され、重いシーンほど遅くなります——
  **測定の分解能が、測定対象の負荷に比例して落ちる**計器です。
  interval polling にし、上限は「速いマシン」ではなく「遅いマシン」で決める。

### L-45 worktree に張った `node_modules` のリンクが、commit に入った

- **症状**: 並行作業用の worktree で `ln -s <本体>/node_modules node_modules` を張り、
  `git add -A` で commit しました。`.gitignore` の `node_modules/` は**ディレクトリ**にしか
  効かず、symlink はファイルなので拾われます。その commit を本体で cherry-pick すると、
  git は ignore 対象の実ディレクトリを**消して**、自分自身を指すリンクに置き換えました。
  `node --test` は依存を使わないテストだけ走らせていたので緑のままでした。
- **どう見つかったか**: cherry-pick の出力に `create mode 120000 node_modules` が
  あった。読んでいなければ、次の `npm run build` まで気づきません。
- **いま何が捕まえるか**: `tests/tree-hygiene.test.js` — 追跡ファイルに
  `node_modules` と symlink（mode 120000）が無いこと。`.gitignore` は
  `node_modules`（スラッシュ無し）も持ちます。
- **一般形**: **ignore は「そのパスの種類」にしか効かない。** 道具のために
  木の中へ置いたものは、`git add -A` の前に `git status --short` で見る。

### L-15 確かめる前に「バグだ」と言った

- **症状**: `#/terms` → `#/privacy` が壊れていると報告した。ブラウザで main と
  branch を A/B したところ、**main は正しく遷移していた**。バグは存在しなかった。
- **どう見つかったか**: 主張したあとで A/B を撮った。順序が逆でした。
- **いま何が捕まえるか**: **人だけ**。撤回はコードのコメント・commit・PR body の
  3 か所に残した（報告した場所すべてに残す、が規則）。
- **一般形**: **「壊れている」は測定結果であって、読解の結論ではありません。**
  A/B を撮ってから言う。

### L-16 F 番号が、並行する branch と衝突し続けた

- **症状**: F-107 / F-108 / F-110 / F-117 / F-119 が、作業中に main 側で先に使われた。
  台帳の冒頭に「次は F-NNN」と書いてあっても、並行 branch には届きません。
- **再発（2026-09-17、L 番号で）**: #122 と #123 が L-31・L-32 を別々の branch で
  同時に確保し、**両方とも単体では緑**のままマージされて、
  **main の `npm test` が赤になりました**。#123 は main を取り込んだ時点で
  採番し直していますが、そこに #122 はまだ載っていません。
- **どう見つかったか**: F 番号は 5 回ともマージ時の競合。L 番号は違って、
  **競合しませんでした**——別々の行を足すだけなので git は黙って両方通します。
  気づいたのは次の作業で main から branch を切って `npm test` を走らせたときです。
- **直す側でも 2 回転びました。** (1) 衝突した 2 件を**一括置換**で動かしたら、
  `docs/follow-ups.md` の中の**相手側の L-31 / L-32 への参照**まで書き換えました
  （3 か所、Codex が指摘）。衝突とは「同じ綴りが 2 つの別物を指して同居している」
  状態なので、置換はまさに区別すべきものを区別しません（L-02 と同じ形）。
  (2) 直している最中に **#127 が main 側で逆向きに解決**しました——
  相手側を動かし、こちらを L-31 / L-32 に残す形です。
  **後からマージする側は、自分の採番し直しを丸ごと捨てて main に合わせる**
  しかありません。番号を動かす作業は、それ自体が競合します。
- **いま何が捕まえるか**: `scripts/lessons.mjs`（`npm test` からも走ります）が
  重複番号を名指しで落とします。**ただし両方が main に載ってからだけ**です——
  検査は自分の木しか見えないので、**どちらの PR でも緑になります**。
  F 番号の側は依然 **人だけ**（`follow-ups.md` に同種の検査はありません）。
  参照が「実在するが別物を指している」ことも **人だけ**——
  どちらの番号も実在するので、リンク切れ検査では捕まりません。
- **一般形**: **番号はマージ直前に取り直す。** 衝突したら branch 側が動く
  （main は既に参照されているため）。**「両方がマージされるまで赤にならない
  ガード」は、赤を見る場所が main しかありません。** そして
  **採番し直すときは一括置換を使わず、参照を 1 件ずつ読んで動かす。**
  直し始める前に、**main 側で誰かが既に直していないか**を見る。

### L-21 公開一覧を禁じている文書自身が、公開一覧を書いていた

- **症状**: 3 つの面が「いま何を公開しているか」を手書きで持ち、全部ずれました。
  `Explorer.js` の副題（「目標は脳と心臓の 2 つ」——その上に 3 臓器が並ぶ）、
  `CLAUDE.md` の β 節（「公開は 1 件」「`heart-anatomy` はまだ存在しない」——
  どちらも誤り）、`locked-surface` の過去の文（「β は脳と心臓の解剖」——公開は脳だけ）。
  **CLAUDE.md のその節は、3 段落下で「公開シーンの一覧をどこかに書き写さないで
  ください」と書いています。**
- **どう見つかったか**: 肝臓を公開した PR のレビューが Explorer の矛盾を指摘。
  同じ形を探して他の 2 件が出ました。
- **いま何が捕まえるか**: `tests/public-ui.test.js` が Explorer の β 文面を
  ソースで読み、臓器名がリテラルで書かれていたら落とします
  （元の文面を戻して「Brain」で赤を確認済み）。`CLAUDE.md` は**人だけ**。
- **一般形**: **禁止を書いた文書は、自分がその禁止を守っているか確かめる。**
  「ここに一覧を書くな」と書く場所は、一覧を書きたくなる場所でもあります。
  数えたいときは `npm run verify:site` の `publishes N` を読む。

### L-27 「戻したはず」を根拠に、シーンを 1 日ぬれぎぬで立たせた

- **症状**: `verify:anatomy` が肩で赤くなり、**run ごとに別のことを言いました**。
  「after Show all, clicking the model selected nothing — it did not come back」、
  「recolouring left 0 rows marked selected in the tree」、そして
  シーンがまだ保持している選択を「失われた」と報告するもの。
  そのうち 1 件を **F-127 として「機能不具合です」「シーンが正しくありません」と
  台帳に書きました**。3 つとも同じ 1 つの原因で、**製品の欠陥ではありません**。
- **どう見つかったか**: シーンを疑って `showAllHiddenStructures` を読み、
  何も見つからず、**やっと実測しました**。Show all の直後に `window.__app` から
  読むと `hidden=0`、20 構造すべて opacity `1.00`、section plane 無し、
  `elementFromPoint` は `CANVAS`——**モデルは完全に戻っています**。
  同じ瞬間にサンプルし直すと 6 点中 5 点がモデルの上。外れているのは
  検査がクリックしていた 1 点だけで、それは**ドラッグの前**に測った点でした。
  カメラは `(-3.60, 2.20, 6.60)` から `(-2.23, 3.38, 4.24)` へ——
  **関節の 1/3 周ぶん**動いています。
  検査のコメントは「**正確に逆にたどる**ので、カメラは戻る」と書いていました。
  controls は damping するので、同じ経路を返しても同じ回転は返りません。
- **いま何が捕まえるか**: `scripts/check-anatomy-interaction.mjs` の
  `liveModelPoint()` が、クリックする各地点をその場で測り直します（`#112` が
  同じ原因を独立に見つけて足したもの。こちらが書いた `settleCamera` /
  `remeasure` は同じ車輪の 2 本目なので捨てました）。
  ただし**「1 日ぬれぎぬを着せた」ほうを捕まえるのは人だけです。**
  残っていた嘘のコメントは同ファイルの drag ステップで実測値に置き換えました——
  「戻らない。だから下は誰も戻ったと仮定してはならない」と、肩の 2 つの座標つきで。
- **一般形**: **「元に戻したはず」は測定ではありません。**
  そして——**赤を 1 回見ただけで原因を名指ししない**（L-15 の再発です）。
  シーンのコードを読み始めてから実測するまで丸 1 日かかり、
  その間ずっと台帳には「シーンが正しくありません」と書いてありました。
  読むのは仮説を作る作業で、**実測だけが仮説を殺せます**。
  順番を逆にすると、疑った場所に必ず「それらしい容疑者」が見つかります。

### L-28 「他より細い」と書いたとき、その「他」を測っていなかった

- **症状**: F-134 を「安全域フィット（#112）のあと、膝と肩が枠の 1 割の細片に
  なった」と書きました。**「1 割」は測った値ですが、「他の臓器と比べて」は
  測っていません。** 比較対象を 1 件も測らずに、退行として台帳に載せました。
- **どう見つかったか**: 直そうとして原因を探す前に A/B を取ったからです。
  同じ計器で #112 前のビルドを測ると膝は 0.42..0.58（幅 0.16）、後は 0.10。
  ところが**帯（パネルが覆う右を除いた部分）の取り分が約 0.72** なので、
  0.16 × 0.72 ≈ 0.115 ——**細くなった分のほとんどは #112 の意図どおり**でした。
  本当の指摘は別のところにあり、**同じビルドの公開臓器**が肝 0.26・肺 0.28 で、
  膝と肩の 0.10 は **2.6 倍細い**。これは #112 の前からある差です。
  つまり最初に書いた「いつから」も「何と比べて」も間違っていて、
  **残った事実は 1 つだけ**でした。
- **いま何が捕まえるか**: **人だけ。** `verify:anatomy` は各シーンの
  `spans` を出しますが、**シーン間で比べる検査はありません**。
  F-134 の完了条件に基準値（公開臓器の 0.26〜0.28）を書いたので、
  次に触る人は比較対象を持ちます。
- **続き（同日、3 回目の書き直し）**: 比較対象を測ってもまだ足りませんでした。
  **`modelSpan` が何を測る関数なのかを、引用する前に読んでいなかった**からです。
  `fy` を 1 つ取って `fx` を掃くので、返るのは**その 1 行の断面**です。
  呼び出し側は `0.45` を渡しています。膝は最大幅が 0.50 の行にあり、肩は
  **0.24 の行**（鎖骨と肩甲骨のあたり）にあるので、0.45 の断面同士を比べることは
  「幅を比べること」ではありませんでした。Codex が #117 で指摘しています。
  シルエットで測り直すと **2.6 倍細いは消えて 1.75 倍と 1.27 倍**になり、
  さらに**高さは関節のほうが使っている**（膝 8/9 行・肩 7/9 行 対 肝 5/9・肺 4/9）。
  幅 × 行数はほぼ同じで、関節は小さいのではなく**縦長**でした。
  **F-134 は指摘ごと消えました。**
- **一般形**: **比較を主張したら、比較対象を測る。そして、測定関数が何を返すのか
  読んでから引用する。**
  「A は B より悪い」の B を測っていない指摘は、A の測定値が正しくても
  **指摘としては未測定**です。とくに「〜のあと悪くなった」は 2 つの測定
  （前と後）を要求しており、片方しか無いなら書けるのは現在値だけです。
  そして**同じ関数から出た 2 つの数は、同じ量とは限りません**——
  形の違う被写体に同じ 1 行を当てれば、比は形の比であって大きさの比ではない。
  L-27 が「1 回の赤で原因を名指しするな」なら、これは
  「**1 つの測定で比較を名乗るな。その測定が何なのかを読まずに名乗るな**」です。

### L-29 検査が「どこで失敗したか」を間違えて報告し、その嘘を台帳に書いた

- **症状**: 腎の drive が `the drive stopped while opening the Display tab:
  locator.click: Timeout 30000ms exceeded.` を返しました。2 run 連続だったので
  **機能不具合として F-135 を立てました**——「『表示』タブが押せない」と。
  実測すると**そのタブは 1275ms で普通に押せます**。止まっていたのは
  **電話サイズ（375×667）でシートを開いたあとの一覧の行**で、
  `<canvas>` が pointer events を奪っていました。**まったく別の場所です。**
- **どう見つかったか**: タブの bounding box・computed style・
  `elementFromPoint` を測ったら**全部正常**だったから。可視・viewport 内・
  `pointerEvents: auto`・中心はボタン自身の span。それで「押せないはずがない」と
  なり、click を try/catch で包んで**エラー全文**を出したら、
  待っていた locator が `.anatomy-tree-leaf:visible` だと分かりました。
  失敗報告は `at()` に最後に渡されたラベルを使いますが、
  **電話の節はラベルを更新していません**。1365 行の「opening the Display tab」が
  1657 行の失敗に貼られていました。
- **いま何が捕まえるか**: `scripts/check-anatomy-interaction.mjs` の catch が、
  失敗の 1 行に **Playwright が実際に待っていた locator** を併記します
  （`waiting for locator(...)`）。ラベルが古くても、読む人には本当の対象が見えます。
  電話の節にも `at()` を 2 つ入れました。
- **一般形**: **「どこで落ちたか」は、検査が自分で名乗った場所ではありません。**
  ステップ名は人間が書いた文字列で、コードの移動や節の追加で簡単に古くなります。
  落ちた場所を知りたいなら、**機械が持っている事実**——待っていた locator、
  スタック、call log——を報告に含めること。
  そして **stderr に出ている全文を grep で捨てない**：
  この件では原因は最初から出力されていて、
  こちらが `problem(s)` の行だけを見ていました。

### L-30 「どの行でも一番広い」を測る掃引が、比較相手の行を見ていなかった

- **症状**: L-28 の続きとして入れた `modelSilhouette`（`--silhouette`）は
  「枠のどこであれモデルが一番広くなる幅」を返す計器です。掃く行は
  `[0.15, 0.24, 0.33, 0.42, 0.5, …]` で、**`0.45` が入っていませんでした**。
  ところが同じ実行が数行下で `modelSpan(0.45)` を出力し、note には
  「シーン間の比較はこちらで行い、下の中央行では行わないこと」と書いています。
  膝では**シルエットが 0.12、中央行が 0.14** ——「どこでも一番広い」が、
  自分が見ていない行より狭い値を、比較用の正解として印字していました。
  同じ関数の粗掃引→再掃引の窓も `coarseBest - 0.04` で、
  **4% 格子が両端で落とす量は 8%** なので、粗で 5% 負けた行——本当は
  一番広いかもしれない行——を再掃引せずに捨てます。
  さらに `for (let fx = 0.02; fx <= 0.96; fx += 0.02)` は浮動小数の累積で
  0.9600000000000003 になり、**0.96 の列を一度も見ていません**
  （掃引は 0.94 で終わり、右端に届くモデルは常に 2% 狭く報告されます）。
- **どう見つかったか**: レビュー（コード読み）です。実行では見つかりません——
  どの値も「もっともらしい数」として印字され、赤くなる検査はありません。
  L-28 が「測定関数が何を返すのか読んでから引用しろ」だったのに対し、
  こちらは**その教訓を実装した新しい計器が、同じ種類の嘘を持って生まれていた**
  という形です。
- **いま何が捕まえるか**: **人だけ。** `modelSilhouette` は Playwright の
  drive の中にあり、単体テストがありません。直したのは
  `scripts/check-anatomy-interaction.mjs` の 3 か所——掃く行に `0.45` を入れ、
  再掃引の窓を `- 0.08` にし、掃引の上限に許容差を付けました。
  掃引を純関数として切り出して `node --test` に載せるまでは、次の同種の誤りも
  レビューでしか止まりません。
- **続き（同日、直し方のほうが間違っていた）**: `0.45` を掃く行の集合に足して
  直したつもりでしたが、**その行は `0.42` の 0.03 下**です（他の間隔は 0.08〜0.09）。
  掃いた行数は「枠の高さをどれだけ使うか」の推定に使っているので、
  不均等な格子にすると**密度加重の数え上げ**になります。実際、測り直した
  4 シーンが**揃って +1 行**になりました——4 件の測定が一致したのではなく、
  全員が横切る帯に標本を 1 つ足した署名です（Codex 指摘）。
  行の集合を 2 つに分け、**等間隔の 9 行だけを占有率に数え**、幅の候補にだけ
  `0.45` を足して 3 回目を測ると、幅も行数も初出の値に戻りました。
  **1 回目の嘘を直す修正が、2 種類目の嘘を持ち込んでいた**わけです。
- **一般形**: **A と B を並べて印字する計器は、A の定義域が B を含むことを
  自分で保証すること。** そして**比率の分母に使う標本は等間隔にすること**——
  片方の質問（幅）のために足した標本が、もう片方の質問（高さの占有率）の
  分母を汚します。質問が 2 つあるなら標本集合も 2 つです。 「どこでも一番広い」と「ある 1 行の幅」を同じ出力に
  並べるなら、前者の標本に後者の行が入っていなければ、前者は後者に負けます。
  そして**格子で測った量を格子の刻み 1 つで比較しない**——量子化誤差は
  両端に乗るので窓は 2 刻みです。
  数値の累積ループは `<=` の右辺に許容差を置く。

### L-42 「いくつ指せるか」を、4 点がどこに落ちたかで数えていた

- **症状**: F-126 は「腎は開いた視点からクリックで指せる構造が 2 つしかない」と
  書き、2 回の測定で「成立したまま」と確認しました。3 回目にシーンへ直接
  聞いたら——`structures.filter(x => !x.hidden && x.currentOpacity > 0.14)`——
  **開始時点で 5 つ**、層 0.7 で **31**、冠状断面ビューでは **32 全部**でした。
  「2」は押せる構造の数ではなく、**drive の 4 点がどこに落ちたか**です。
  うち 2 点は、意図的に 1 構造として置かれた右腎に当たっていました。
- **どう見つかったか**: 直そうとして「では何を変えれば増えるのか」を測ったから。
  **指摘を確認する 2 回の再測定では見つかりませんでした**——どちらも同じ
  4 点を数え直しており、同じ答えが 2 回出たことを「確認」と呼んでいました。
  同じ計器で 2 回測っても、計器が間違っていれば 2 回とも同じだけ間違います。
- **いま何が捕まえるか**: `npm run points:anatomy`
  （`scripts/measure-anatomy-points.mjs`、`--scene <slug> --preview --dense`）
  が、掃引ごとに **`N structure(s) reached`** を出します。`--layer` / `--view` で
  状態を指定できるので、「この構図は何個指させるのか」を 3 状態ぶん同じ計器で
  測れます。ただし **N は下限で、しかも枠の一部についての下限**です——格子は
  **キャンバス全体でも中央でもなく**、x 0.14→0.74 / y 0.18→0.75 の矩形で、
  **右 1/4 と下 1/4 には 1 点も置いていません**（加えて刻みより細い構造は
  2 点のあいだに落ちます）。だから各行が掃引した矩形を一緒に出します。
  「**掃引した矩形の中で少なくとも N**」と書き、「全部で N」とも
  「枠の外だから指せない」とも書かないこと。
- **訂正（2026-09-17）: この欄が最初に提案した機械化も間違っていました。**
  ここには「上の 1 行（hidden でなく opacity > 0.14 の数）をフラグとして
  drive に足せば機械化できる」と書いてありました。**その 1 行は `_isPickable` の
  一節を再現しただけで、数えているのはレイキャスト候補です。** 画角の外の構造も
  （腎は `tract` タグをフィット対象から外すので、尿管と膀胱は枠外でも数に入る）、
  手前の面に隠れた構造も、そのまま数に入ります。Codex の指摘で分かりました。
  **実際にポインタで掃引して測ると、開始時点の腎は 4 種類**（皮質・右腎・左右尿管）
  で、5 でも 2 でもありませんでした。つまり L-42 は、間違った数え方を、
  別の間違った数え方で訂正していました。
  そして**その道具は既にありました**（`points:anatomy`）。
- **訂正の訂正（2026-09-17、同日のレビュー）: 上の「膀胱は枠の外にいて、
  指せません」も、掃引が測れる範囲を超えた主張でした。** 格子は y 0.75 で
  終わっており、膀胱が描かれるのはその下の**1 点も置いていない帯**です。
  **計器は「枠の外」と「掃引の外」を区別できません。** 4 回目の数え方は
  3 回目より良いが、そこから出した結論の 1 つは、また計器の形を
  母集団の形として読んだものでした——**同じ誤りの 4 回目**です。
  道具の側を直しました（各行が掃引した矩形を出す）。
- **一般形**: **標本の性質を、母集団の性質として書かない。**
  4 点が 2 種類を指したことは 4 点についての事実で、シーンについての事実では
  ありません。「N しか無い」と書く前に、**N を数える方法が標本と別にあるか**を
  問うこと。無いなら、書けるのは「標本では N だった」だけです。
  そして**同じ計器での再測定は独立した確認ではありません**（L-28 の系）。

### L-39 当たらないフィルタを「空集合」として受け入れ、0 枚撮って exit 0 した

- **症状**: 腎の開始構図を A / B / C の 3 案で撮り比べようとして、
  `capture-anatomy-views.mjs --view kidneys` を 2 回、`--view coronal-section` を
  1 回走らせました。**B だけ絵が出て、A と C は空のディレクトリ**でした。
  終了コードは 3 本とも **0**、要約行は 3 本とも
  `6 viewpoint(s) x 2 colour mode(s)` です。`--view` が名指しするのは
  ボタンの**ラベルを slug にしたもの**で、`static views` の `id` ではありません
  ——腎の `coronal-section` は偶然どちらでもあり、`kidneys` は id だけで、
  ラベルは `both-kidneys` に落ちます。当たらない id は `continue` され、
  **何も撮らなかったことは、どこにも出ませんでした。**
- **どう見つかったか**: 絵を見に行ったらディレクトリが空だったから。
  **要約行を信じていたら気づきません**——それは「シーンが提供する数」を
  出しており、「撮った数」ではないので、**0 枚のときも満額を報告します**。
  同じ script の `--recipe` は最初から「無い id は die、持っている id を並べる」で、
  その規則が views と modes に伝わっていませんでした。
- **いま何が捕まえるか**: `scripts/capture-anatomy-views.mjs` が、
  `--view` / `--mode` に当たらない値が来たら **`die`** し、提供している slug を
  並べます。要約行は**撮った枚数**（`N image(s)`）になりました。
  `scripts/measure-anatomy-points.mjs` の `--view` も同じ規則で入れてあります。
- **一般形**: **フィルタの当たらなさは、空の結果ではなく入力の誤りです。**
  そして**計器の要約は、材料ではなく成果物を数えること。**「何を提供しているか」を
  報告する行は、**何もしなかった実行でも満額を出します**。

### L-41 ローカルの緑は branch の緑で、**これから載る状態の緑ではなかった**

- **症状**: この branch は `npm test` 2597/2597 で緑、`npm run lessons` も緑。
  それでも CI の `test-and-build` が**赤**になりました。原因は branch の中には
  無く、**main が進んだこと**です——同じ番号衝突を別 PR（#127）が**逆向きに**
  直してマージされ、こちらの採番と合わせると `L-36` / `L-37` が 2 組できます。
  **番号衝突を直す修正どうしが、もう一段上で衝突しました。**
- **どう見つかったか**: CI だけ。ローカルでは 3 回走らせて 3 回とも緑です。
  しかも TAP の `not ok` 行はログの先頭側にあり、`get_job_logs` は末尾しか
  返さないので**失敗したテスト名は最後まで読めませんでした**。
  効いたのは `# skipped 1` の差（CI には `dist/` が無い）を潰したうえで、
  **`git fetch origin main` して「main は進んだか」を見たこと**です。
- **いま何が捕まえるか**: `.github/workflows/ci.yml` が `pull_request` の
  **merge ref**（branch と現在の main を合わせたもの）で走るので、CI は捕まえます。
  **ローカルには何もありません**——手元で同じものを測りたければ、
  `git fetch origin main && git merge origin/main` を**テストの前に**置くこと。
- **同じ日にもう 2 回起きました。** main を取り込み直したら、別の branch が
  **`L-40` を既に取っており**（#125）、**`F-138` も二重**になっていました
  ——こちらは腎の断面、main 側は「公開前に private へ戻す」。
  L 番号には L-16 以来ガードがありますが、**F 番号には 1 つもありませんでした。**
  turning it on の初回で、**過去ぶんを含めて 22 件**の重複が出ています。
- **いま何が捕まえるか（追記）**: `npm run follow-ups`
  （`scripts/lib/follow-ups.mjs`、`npm test` からも走ります）が F 番号の重複を
  見ます。既存 21 件は `tests/follow-ups-duplicates-baseline.json` に**正確に**
  記録し、**新しい 1 件で赤**になります（`--write` は baseline を**増やす方向には
  書きません**）。type-floor と同じ形で、同じ理由です——数を緩めて緑にするのではなく、
  借金を見えるところに置いて、増やせなくする。
- **一般形**: **「緑です」と書くときは、何の緑かを言う。** 手元の緑は
  「私の変更だけ」の緑であり、載るのは「私の変更＋その間に動いた main」です。
  main が動く PR ほど、この 2 つは離れます。**並行して同じ台帳・同じ採番を
  触っている修正があるときは、マージ直前にもう一度 fetch すること**（L-16 の系）。
  そして**採番が衝突する台帳には、必ず重複ガードを置くこと。**

