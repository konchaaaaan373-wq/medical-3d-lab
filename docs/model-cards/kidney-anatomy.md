# Model card — Interactive kidney anatomy

| | |
| --- | --- |
| **Scene** | `kidney-anatomy` |
| **Geometry** | [`src/scenes/renal/organs/kidney.js`](../../src/scenes/renal/organs/kidney.js), [`src/scenes/renal/organs/kidneyAnatomy.js`](../../src/scenes/renal/organs/kidneyAnatomy.js) |
| **Naming adapter** | [`src/data/kidneyAnatomyScene.js`](../../src/data/kidneyAnatomyScene.js) |
| **Selection behaviour** | [`src/scenes/shared/anatomy/OrganAnatomyScene.js`](../../src/scenes/shared/anatomy/OrganAnatomyScene.js), [`src/scenes/renal/scenes/kidneyAnatomy/KidneyAnatomyScene.js`](../../src/scenes/renal/scenes/kidneyAnatomy/KidneyAnatomyScene.js) |
| **Tests** | [`tests/kidney-anatomy.test.js`](../../tests/kidney-anatomy.test.js), [`tests/organ-anatomy-scenes.test.js`](../../tests/organ-anatomy-scenes.test.js) |
| **Evidence** | [`docs/model-evidence/kidney-anatomy.md`](../model-evidence/kidney-anatomy.md) |

## 1. What question this model answers

**How is a kidney arranged from the capsule inwards, and where does the urine
one pyramid makes actually go?**

## 2. What it is

An interactive gross-anatomy model of the urinary tract, built in code. The
left kidney is opened into its parts: the **cortex as one shell** between the
capsule and the corticomedullary junction, and inside it **seven medullary
pyramids** with the **cortical columns** between them and three medial-margin
parts where the bean turns in towards the hilum. From each papilla the
collecting system is drawn outwards — a **minor calyx** cupping each papilla,
**three major calyces** gathering them, one **pelvis**, and the **ureter**
leaving at the hilum for the **bladder**. **32 structures are selectable.**

**Only one kidney is opened.** Carving a kidney into parts costs about a
second, and two of them is two seconds of a frozen page for a second copy of a
partition the reader has already seen. The right kidney is built as the
landmark shape it is and declared as **one** structure, with a note saying it
is not partitioned. It is never given part names: a name with no boundary under
it is the failure this layer exists to avoid.

**A renal column is cortex, not medulla.** It is the same tissue as the shell
outside the pyramids, which is why the builder gives it the cortex's colour and
why the interlobar vessels — not drawn here — run in it.

## 3. What it is not

- **Not a vascular model.** No renal artery, no segmental, interlobar or arcuate
  branches, no vein. `src/catalog/anatomy.js` records that as the kidney's next
  step towards A3.
- **The sinus is not carved out.** The pyramids converge where the sinus is and
  the collecting system is drawn in that convergence. There is no "renal sinus"
  to select, because there is no such mesh.
- **One coronal row, not two.** A kidney's pyramids form an anterior and a
  posterior row; these seven are one row in the coronal plane.
- **The corticomedullary junction is the capsule scaled**, not a surface of its
  own.
- **No nephron.** `renal-filtration` and `urinary-filtration` are where the
  nephron and the physiology are; nothing here is solved.
- **The ureter is illustrative.** Its calibre and course are drawn to read
  clearly, and the three normal narrowings are not modelled.

## 4. Sources and licence

All geometry is generated by this repository from the arrangement declared in
`kidneyAnatomy.js`. No external 3D asset, no imaging, no patient data.

## 5. Accuracy and uncertainty

What the tests hold: the cortex as one shell between the capsule and the
junction; seven pyramids and the columns between them partitioning what is
inside that shell, with no gap and no overlap; a minor calyx cupping each
papilla; three major calyces gathering the seven; the pelvis leaving at the
hilum; and a nephron placed across the scales — glomerulus in the cortex, loop
of Henle towards the papilla.

What is not held: any dimension in centimetres, the number of pyramids in an
individual kidney (seven is a common arrangement, not a constant), and the
position of either kidney relative to the ribs or the vertebrae.

## 6. Presentation choices

- **The scene says what frame shape it needs, and what it is about.** The
  framing is set on the two kidneys, not on the tract they drain into: a frame
  that fits the bladder makes the organ the scene is named after a third of the
  size. "Urinary tract" is the viewpoint that offers that trade deliberately.
- **The opening view sits a little further back than the pair needs.** At the
  distance that fitted them exactly, the two kidneys ran edge to edge with the
  gap between them down the middle of the frame — the one arrangement in which
  a first click lands on nothing.
- **The layer slider works in three steps**: the cortex fades so the pyramids
  and columns show through it, then the parenchyma fades so the calyces and the
  pelvis show through that. Nothing moves.
- **The coronal section cuts** — the view every kidney diagram is drawn from —
  and is labelled as a cut rather than as a fade.
- **Colour separates cortex from medulla from urothelium.** "Natural tissue"
  puts a single tissue reading back. Neither is measured tissue colour.
- **The frame follows the viewpoint.** The camera fits the organ into the part
  of the window nothing is covering — the parts panel is an overlay over the
  canvas, not a narrower canvas — and a viewpoint that hides a side or cuts the
  organ open is framed on what it leaves rather than on the whole model.
- **A cut draws the face it leaves, where there is one to draw.** For a solid
  part the cross-section is computed from the triangles the plane crosses and
  drawn in that part's own colour, so a cut segment reads as tissue with a
  surface rather than as a shell seen from the inside. For a hollow one — a
  stomach, a duct, a loop of bowel — the cut opens it instead: the wall here is
  a surface with no thickness, and facing it would draw a lumen as a lump of
  tissue. Which a part is, is declared by the scene rather than guessed.
- **Isolating a structure shows it solid.** "Show me only this" used to leave
  the part at the opacity it has in place, which is right for the opaque ones
  and useless for the see-through ones — and a structure you cannot see in
  place is exactly the one a reader isolates. Nothing else is drawn, so there
  is nothing left for it to be transparent against.
- **Taking a structure out of the way is the reader's own way in.** The layer
  slider, and where this scene has them the cuts and the viewpoints, are its
  *authored* ways of showing what is inside; hiding is the one the reader
  chooses — one structure, or a whole branch of the part tree in a single
  press. It asserts nothing anatomical and moves nothing: a hidden structure is
  still there, still listed, still findable by name, and **Unhide all** brings
  every one of them back. While it is off screen it also leaves the picker, so
  a ray cannot select what the reader cannot see. Hiding the structure that is
  currently isolated ends the isolation, because "only this one" and "not this
  one" cannot both be true, and that ending is announced rather than left for a
  panel to notice. In the other order — isolating something already hidden, by
  the reader or by the viewpoint — **isolation wins outright**: it is a
  temporary override that writes nothing down, so clearing it hands back the
  viewpoint and the hidden set exactly as they were. Resolving the three as an
  order rather than as one "or" is what keeps a model on screen at all; OR'd,
  isolating a structure the viewpoint was already hiding hid every structure in
  the scene.

## 7. What it must never be used for

Diagnosis, treatment selection, dose selection, prognosis, or planning any
procedure — biopsy, nephrostomy, stone localisation or stenting included.

## 8. Review status

**Catalog status:** `alpha`

No clinical sign-off is recorded; the registry entry is `pending`, and the
scene is not part of the public beta.
