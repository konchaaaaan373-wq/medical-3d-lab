node_modules/
dist/
.DS_Store
*.local
.vite/
.env
.env.*
!.env.example

# Throwaway scripts used to drive the browser during a visual pass. What they
# write belongs in `*.local` (ignored above) or in a temporary directory
# outside the repository — three measurement dumps reached a commit because
# they were written beside the driver and matched nothing here.
.*.mjs

# The viewport matrix report, written by `npm run verify:ui --json`.
viewport-report.json

# Written by `npm run cards:check` while it compares against what is committed.
.social-check/

# Candidate assets a scene under development fetches. Pinned in
# src/catalog/devAssets.js and fetched by `npm run assets:dev`; never committed —
# a third-party binary does not belong in this repository.
dev-assets/
dist-preview
