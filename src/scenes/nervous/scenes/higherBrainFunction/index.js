export { HigherBrainFunctionScene as default, HigherBrainFunctionScene } from './HigherBrainFunctionScene.js';
/**
 * What a named structure is for, for the anatomy surfaces that ask.
 *
 * Re-exported from the scene's entry point so the application reaches it the
 * same way it reaches the scene: through the catalogue's own loader, which a
 * production build replaces with a rejecting thunk for a scene the release does
 * not open. See `structureFunctions.js`.
 */
export { functionNoteForSelection } from './structureFunctions.js';
